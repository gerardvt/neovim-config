-- Fidget shows LSP progress messages (startup spinners, indexing status) in a
-- corner overlay. Added to replace the visual feedback Mason previously
-- provided when LSP servers were installing and initializing.
require('fidget').setup({})
