-- --------------------------------------------------------
-- Setup options (prior to plugins load/config).
-- ---------------------------------------------------------
require('config.options')

-- ---------------------------------------------------------
-- Setup key mappings (prior to plugins load/config).
-- ---------------------------------------------------------
require('config.keymappings')

-- ---------------------------------------------------------
-- Plugins management via lazy.nvim
-- ---------------------------------------------------------
require('config.lazy-plugins')
